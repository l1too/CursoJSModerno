const producto = "         Monitor 40       "

console.log(producto.length)

// eliminar del inicio
console.log(producto.trimStart().length)
// eliminar del final
console.log(producto.trimEnd().length)
// eliminar los dos
console.log(producto.trimStart().trimEnd().length)
