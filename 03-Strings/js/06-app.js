const producto = "Monitor 40 pulgadas"

let texto = "en promocion "
// repite una cadena de texto
console.log(texto.repeat(2))

// divide una cadena de texto por el simbolo o letra que se le pase, y los guarda en un Array
const actividad = "Estoy aprendiendo javascript"

let asd = actividad.split(" ")
for(i = 0 ; i < asd.length ; i++){
    console.log(asd[i])
}