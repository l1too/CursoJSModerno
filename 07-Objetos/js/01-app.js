const nombre = "Monitor 20 pulgadas"
const precio = 300
const disponible = true

// Guardamos las variables en un objeto...

// Objeto literal

const producto = {
    nombre: "Monitor 20 pulgadas",
    precio : 300,
    disponible : true,
}

console.log(producto)